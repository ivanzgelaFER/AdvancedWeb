{
  "name": "pwa-examples",
  "version": "1.0.0",
  "description": "PWA examples used in lectures in Advanced web developmen course @FER",
  "main": "server.js",
  "scripts": {
    "ver01": "SET VER=01 && nodemon --inspect server.js",
    "ver02": "SET VER=02 && nodemon --inspect server.js",
    "ver03": "SET VER=03 && nodemon --inspect server.js",
    "ver04": "SET VER=04 && nodemon --inspect server.js",
    "ver05": "SET VER=05 && nodemon --inspect server.js",
    "ver06": "SET VER=06 && nodemon --inspect server.js",
    "gen-vapid": "web-push generate-vapid-keys" 
  },
  "author": "imekterovic",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "fs-extra": "^10.0.0",
    "multer": "^1.4.3",
    "nodemon": "^2.0.12",
    "web-push": "^3.4.5"
  }
}
